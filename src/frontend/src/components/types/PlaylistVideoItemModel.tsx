export interface PlaylistVideoItemModel {
  id: string;
  videoUrl: string;
  videoTitle: string;
  thumbnailUrl: string;
  provider: string;
  accountId: string;
  userName: string;
  createdAt: string;
}
